# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Supabase CLI local link/cache state, not project source
supabase/.temp

# Environment files — NEVER commit API keys
.env
.env.local
.env.*.local
*.local

# VIP/door check-in data and ticket images — guest PII and scannable QR
# codes; live data lives in Supabase, these are local generation artifacts
generated_tickets/
public/vip table one/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
